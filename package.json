{
  "name": "@local/agent-delegator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "agent-delegator": "src/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "bun build src/cli.ts --target=bun --outfile=dist/agent-delegator"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "bun-types": "^1.3.13"
  },
  "dependencies": {
    "ajv": "^8.20.0"
  }
}
